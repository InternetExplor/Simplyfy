<?xml version="1.0" encoding="utf-8"?>
<!--
  Виджет ставится во всю ширину экрана.
  targetCell* — то, что читают лаунчеры на Android 12+; ширину берём с запасом,
  лаунчер сам подрежет её до числа колонок своей сетки, а это и есть вся ширина.
  minWidth/minHeight остаются для старых лаунчеров (70*n - 30 на клетку),
  minResize* — нижняя граница, если виджет захотят ужать вручную.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="320dp"
    android:minHeight="180dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="6"
    android:targetCellHeight="3"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_today"
    android:previewLayout="@layout/widget_today"
    android:resizeMode="horizontal|vertical"
    android:description="@string/widget_name"
    android:widgetCategory="home_screen" />
